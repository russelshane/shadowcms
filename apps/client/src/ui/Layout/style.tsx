/**
 * Styles for Layout UI Component
 *
 * @author ShadowCMS
 */

import styled from 'styled-components';

export const LayoutContainer = styled.div({
  margin: 0,
  padding: 0,
  width: '100%',
  minHeight: '100vh',
  boxSizing: 'border-box',
});
